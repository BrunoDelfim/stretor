# ============================================
# Stretor - Variáveis de Ambiente
# ============================================

# --- Aplicação ---
APP_NAME=Stretor
APP_ENV=local
APP_DEBUG=true
APP_KEY=
APP_URL=http://localhost

# --- HTTP (Nginx) ---
HTTP_PORT=80

# --- Frontend (Vue/Vite) ---
FRONTEND_PORT=5173
VITE_API_URL=http://localhost/api
VITE_MEDIA_SERVICE_URL=http://localhost/media

# --- Media Service (Node.js + FFmpeg) ---
MEDIA_SERVICE_PORT=3000
NODE_ENV=development

# --- PostgreSQL ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=stretor
POSTGRES_USER=stretor
POSTGRES_PASSWORD=secret

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://redis:6379

# --- Cache / Queue / Session ---
CACHE_STORE=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis

# --- Storage ---
STORAGE_PATH=/app/storage

# --- TMDB (catálogo mundial de filmes) ---
# Obrigatória: chave da API do TMDB usada pelo backend para montar a Home.
TMDB_API_KEY=
TMDB_LANGUAGE=pt-BR
TMDB_REGION=BR
# Teto de páginas da rolagem infinita da Home (padrão 25, ~500 filmes).
TMDB_MAX_PAGES=25
